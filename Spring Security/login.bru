meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@gmail.com",
    "password": "admin3216"
  }
}

vars:post-response {
  accessToken: res.body.accessToken
}

settings {
  encodeUrl: true
}
