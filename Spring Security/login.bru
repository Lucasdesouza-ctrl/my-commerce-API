meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{server}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@email.com",
    "password": "admin"
  }
}

vars:post-response {
  accessToken: res.body.accessToken
}

settings {
  encodeUrl: true
}
